<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <!-- Selection Toolbar (hidden by default) -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/selection_toolbar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:minHeight="44dp"
        android:background="@drawable/selection_toolbar_background"
        android:elevation="4dp"
        android:visibility="gone"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationContentDescription="@string/close_selection"
        app:contentInsetStartWithNavigation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/selection_count"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/multiselect_toolbar_title_format"
                android:textSize="14sp"
                android:textColor="?attr/colorOnPrimary" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/rules_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="@string/firewall_multiselect_toolbar_button_rules"
                android:textColor="?attr/colorOnPrimary"
                style="@style/Widget.Material3.Button.TextButton" />

        </LinearLayout>

    </com.google.android.material.appbar.MaterialToolbar>

    <!-- Filter Chips - Edge to Edge -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="2dp"
        android:paddingBottom="0dp">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/filter_chips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:singleLine="true"
            app:chipSpacing="7dp" />

    </HorizontalScrollView>

    <!-- Search Box with Counter -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="1dp"
        android:layout_marginBottom="9dp">

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/search_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/firewall_search_hint"
            app:startIconDrawable="@android:drawable/ic_menu_search"
            app:endIconMode="custom"
            app:endIconDrawable="@drawable/ic_clear_search"
            app:boxBackgroundMode="outline"
            app:boxCornerRadiusTopStart="12dp"
            app:boxCornerRadiusTopEnd="12dp"
            app:boxCornerRadiusBottomStart="12dp"
            app:boxCornerRadiusBottomEnd="12dp"
            app:hintTextAppearance="@style/TextAppearance.Material3.BodySmall"
            app:hintEnabled="false"
            app:expandedHintEnabled="false"
            app:hintAnimationEnabled="false"
            style="@style/Widget.De1984.TextInputLayout.Compact">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/search_input"
                android:layout_width="match_parent"
                android:layout_height="41dp"
                android:hint="@string/firewall_search_hint"
                android:inputType="text"
                android:maxLines="1"
                android:textSize="14sp"
                android:gravity="center_vertical"
                android:paddingTop="0dp"
                android:paddingBottom="0dp"
                android:paddingEnd="100dp"
                android:imeOptions="actionSearch" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Package Counter - overlaid on the right side of search box -->
        <TextView
            android:id="@+id/package_counter"
            android:layout_width="wrap_content"
            android:layout_height="41dp"
            android:layout_gravity="end|center_vertical"
            android:gravity="center_vertical"
            android:paddingEnd="40dp"
            android:textSize="14sp"
            android:textColor="?android:attr/textColorHint" />

    </FrameLayout>

    <!-- RecyclerView with Loading and Empty States -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/packages_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:focusable="true"
            android:focusableInTouchMode="true" />

        <!-- Loading State -->
        <ProgressBar
            android:id="@+id/loading_state"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:visibility="gone" />

        <!-- Empty State -->
        <LinearLayout
            android:id="@+id/empty_state"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="32dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:src="@drawable/ic_shield"
                android:contentDescription="@string/cd_shield_icon"
                app:tint="?android:attr/textColorSecondary" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/firewall_empty_state_title"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="?android:attr/textColorPrimary" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/firewall_empty_state_subtitle"
                android:textSize="14sp"
                android:textColor="?android:attr/textColorSecondary" />

        </LinearLayout>

    </FrameLayout>

</LinearLayout>

